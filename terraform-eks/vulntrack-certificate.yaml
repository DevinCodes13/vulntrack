apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vulntrack-tls
  namespace: default
spec:
  secretName: vulntrack-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - aws.vulntrack.app
